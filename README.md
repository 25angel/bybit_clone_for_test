# Bybit Clone - Flutter Application

Flutter приложение с идентичным дизайном Bybit для iOS.

## Особенности

- 🎨 Темная тема в стиле Bybit
- 📊 Экран Markets с списком криптовалют
- 📈 Экран Trade с графиком и формой ордеров
- 💰 Экран Wallet с балансом и активами
- 👤 Экран Account с настройками профиля
- 📱 Адаптивный дизайн для iOS

## Установка

1. Убедитесь, что у вас установлен Flutter:
```bash
flutter --version
```

2. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd bybit
```

3. Настройте секреты (см. раздел "Настройка секретов" ниже)

4. Установите зависимости:
```bash
flutter pub get
```

5. Запустите приложение:
```bash
flutter run
```

## Настройка секретов

⚠️ **ВАЖНО**: Перед запуском приложения необходимо настроить следующие файлы с секретами:

### 1. Firebase конфигурация

#### Для iOS:
```bash
# Скопируйте пример файла
cp ios/Runner/GoogleService-Info.example.plist ios/Runner/GoogleService-Info.plist

# Отредактируйте файл и вставьте свои Firebase данные
# Или скачайте GoogleService-Info.plist из Firebase Console
```

#### Для Flutter:
```bash
# Скопируйте пример файла
cp lib/firebase_options.example.dart lib/firebase_options.dart

# Отредактируйте файл и вставьте свои Firebase конфигурационные данные
# Или запустите: flutterfire configure
```

### 2. Bybit API ключи (опционально)

Если вы хотите использовать реальные данные Bybit API:

```bash
# Скопируйте пример файла
cp lib/config/api_config.example.dart lib/config/api_config.dart

# Отредактируйте файл и вставьте свои API ключи
```

**Примечание**: Все файлы с секретами уже добавлены в `.gitignore` и не будут закоммичены в репозиторий.

## Структура проекта

```
lib/
├── main.dart                 # Точка входа
├── theme/
│   └── app_theme.dart       # Тема приложения
├── screens/
│   ├── home_screen.dart     # Главный экран с навигацией
│   ├── markets_screen.dart  # Экран рынков
│   ├── trade_screen.dart    # Экран торговли
│   ├── wallet_screen.dart   # Экран кошелька
│   ├── earn_screen.dart     # Экран Earn
│   └── account_screen.dart  # Экран аккаунта
├── widgets/
│   ├── bottom_nav_bar.dart  # Нижняя навигация
│   ├── crypto_list_item.dart # Элемент списка криптовалют
│   ├── custom_icons.dart    # Кастомные иконки
│   └── background_chart.dart # График на фоне
├── models/
│   └── crypto_model.dart    # Модель криптовалюты
└── services/
    └── crypto_api_service.dart # Сервис для работы с CoinGecko API
```

## Цветовая схема

- Primary Green: `#00D2FF`
- Primary Red: `#F6465D`
- Background Dark: `#0A0E27`
- Background Card: `#131829`
- Text Primary: `#FFFFFF`
- Text Secondary: `#8A8FA3`

## Зависимости

- `flutter_svg` - для SVG иконок
- `google_fonts` - для шрифтов
- `fl_chart` - для графиков
- `http` - для работы с API

## API

Приложение использует **CoinGecko API** для получения реальных данных о криптовалютах:
- **Бесплатный** - не требует API ключа
- **Endpoint**: `https://api.coingecko.com/api/v3/coins/markets`
- **Лимиты**: 10-50 запросов в минуту (зависит от плана)
- При недоступности API используются моковые данные

### Использование API

Данные загружаются автоматически при открытии экранов Markets и Home.
Для обновления данных можно добавить pull-to-refresh (в будущих версиях).

